#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

source "${SCRIPT_DIR}/version"

docker run --rm \
  -u $(id -u):$(id -g) \
  -v "$(pwd)":/data \
  ${MERMAID_CLI_IMAGE} "$@"