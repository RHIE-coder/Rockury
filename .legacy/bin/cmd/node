#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

source "${SCRIPT_DIR}/version"

docker run --rm -it \
  -v "$PWD":/app \
  -w /app \
  "$NODE_IMAGE" \
  node "$@"